export type WorkspaceView = "pipeline" | "clients" | "knowledge" | "project" | "settings";

export type WorkbenchStep = "intake" | "agent" | "quote" | "outcome";

export type StreamState = "idle" | "connecting" | "connected" | "reconnecting" | "settled";
